---
- name: Ping server and create user on CentOS
  hosts: nati-desktop
  become: yes
  tasks:
    - name: Ping the server
      ping:

    - name: Create a new user
      user:
        name: testuser
        state: present
        shell: /bin/bash
        password: "{{ 'Coop#4321' | password_hash('sha512') }}"


